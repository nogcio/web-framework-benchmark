name: Test Rust Benchmark

on:
  push:
    branches: [ "main" ]
    paths:
      - 'benchmarks/rust/**'
      - 'benchmarks_db/**'
      - 'config/benchmarks.yaml'
      - '.github/workflows/reusable-benchmark-test.yml'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'benchmarks/rust/**'
      - 'benchmarks_db/**'
      - 'config/benchmarks.yaml'
      - '.github/workflows/reusable-benchmark-test.yml'

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: axum
          - name: axum-sqlx-postgres
          - name: pingora
          - name: actix-web

    name: ${{ matrix.name }}
    uses: ./.github/workflows/reusable-benchmark-test.yml
    with:
      benchmark: ${{ matrix.name }}
